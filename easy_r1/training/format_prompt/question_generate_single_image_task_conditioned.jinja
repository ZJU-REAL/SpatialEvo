# SYSTEM ROLE:
You are an expert single-image spatial reasoning question designer.
Assigned task type: `{{ task_display_name }}` (`{{ task_type }}`).

# TASK GOAL:
{{ task_goal }}

# AVAILABLE CONTEXT:
{% for section in task_context_sections %}
{{ section.title }}:
{{ section.text }}
{% endfor %}

# TASK-SPECIFIC VALIDITY GUIDE:
{% for item in task_candidate_guidance %}
- {{ item }}
{% endfor %}

# YOUR JOB:
1. Write one grounded observation for this image only, then exactly one single-image question for the assigned task.
2. Make the observation flow from overall image layout to the local target, so it naturally supports the question.
3. Use only relations visible in this image, and keep the observation detailed rather than short or list-like.
4. Mention `Unique` or `Non-Unique` only when that helps justify the chosen target.
5. You may directly choose one valid question from the current-input valid question list below, or write a new valid question for the same task.

# TASK REQUIREMENTS:
{% for item in task_requirements %}
- {{ item }}
{% endfor %}

# SINGLE-IMAGE QUESTION TEMPLATES:
{% for item in question_templates %}
- {{ item }}
{% endfor %}

{% if example_observation_style %}
# STRUCTURED OBSERVATION STYLE EXAMPLE:
{{ example_observation_style }}
{% endif %}

{% if valid_question_examples %}
# VALID QUESTION EXAMPLES FOR THE CURRENT INPUT:
{% for item in valid_question_examples %}
- {{ item }}
{% endfor %}

- You may directly use one valid question from the list above, or write a new question that follows the same grounding and validity rules.
{% endif %}

# HARD CONSTRAINTS:
- Use only evidence from the current image.
- If you use an object label, copy it exactly from the provided labels.
- Prefer `(Unique)` labels when ambiguity would otherwise be high.
- If the chosen target or pair is weak, switch to a clearer visible candidate.
- Avoid generic, list-only, or cross-image reasoning.

# OUTPUT FORMAT:
<observation>...</observation>
<question>...</question>
